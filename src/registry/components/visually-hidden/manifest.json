{
  "id": "visually-hidden",
  "name": "Visually Hidden",
  "description": "Screen-reader-visible utility for icon labels, hidden field labels, skip links, status text, list bounds, and live regions.",
  "kind": "ui",
  "files": [
    { "source": "visually-hidden.tsx", "target": "visually-hidden.tsx", "alias": "ui" }
  ],
  "dependencies": [
    { "name": "class-variance-authority", "version": "^0.7.0", "kind": "runtime" },
    { "name": "clsx", "version": "^2.1.1", "kind": "runtime" },
    { "name": "tailwind-merge", "version": "^2.5.4", "kind": "runtime" }
  ],
  "registryDependencies": ["utils"],
  "tags": ["accessibility", "typography", "primitive"],
  "documentation": {
    "groups": {
      "Visually Hidden": ["icon-label", "field-label", "skip-link", "status", "list-bound", "live-status"]
    },
    "samples": {
      "default": "Screen reader text",
      "perVariant": {}
    }
  }
}
